<%= form_for(pin) do |f| %>
  <% if pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_area :location %>
  </div>

  <div class="field">
    <%= f.label :pin_lat %>
    <%= f.text_field :pin_lat %>
  </div>

  <div class="field">
    <%= f.label :pin_lng %>
    <%= f.text_field :pin_lng %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
